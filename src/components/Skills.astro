---
import Icon from './Icon.astro';
---

<section class="box skills github-stats-container">
	
	<div class="stack gap-2 lg:gap-4">
		<Icon icon="terminal-window" color="var(--accent-regular)" size="2.5rem" gradient />
		<h2>Software Developer</h2>
		<p>The developer is a skilled problem solver with a passion for innovation and a love for coding.</p>
	</div>
	<div class="stack gap-2 lg:gap-4">
		<Icon icon="trophy" color="var(--accent-regular)" size="2.5rem" gradient />
		<h2>Community Volunteer</h2>
		<p>Willingness to help others without expecting anything in return.</p>
	</div>
	<div class="stack gap-2 lg:gap-4">
		<Icon icon="strategy" color="var(--accent-regular)" size="2.5rem" gradient />
		<h2>Strategy-Minded</h2>
		<p>Stay strategy-minded, for it is the compass that guides you to success.</p>
	</div>

</section>

<style>

.github-stats-container {
  position: relative;
  background:var(--github-show-box-background-skills-color);
border: 1px solid var(--gray-800);
		border-radius: 1.25rem;


}



.github-stats-container::after {
  border: 1px solid var(--gray-800);
  content: '';
  position: absolute;
  top: -1px;
  left: -1px;
  right: -1px;
  bottom: -1px;
  background: linear-gradient(105deg, #ffd800, #ff5520, #750cf2, #0cbcf2);
  z-index: -1;
  		border-radius: 1.25rem;

}
.github-stats-container::before {
border: 1px solid var(--gray-800);
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(75deg, #ffd800, #ff5520, #750cf2, #0cbcf2);
  z-index: -2;
  filter: blur(10px);
  		border-radius: 0.85rem;

}

	.box {
		border: 1px solid var(--gray-800);
		
		padding: 1.5rem;
  background:var(--github-show-box-background-skills-color);
		box-shadow: var(--shadow-sm);
	}

	.skills {
		display: flex;
		flex-direction: column;
		gap: 3rem;
	}

	.skills h2 {
		font-size: var(--text-lg);
	}

	.skills p {
		color: var(--gray-400);
	}

	@media (min-width: 50em) {
		.box {
			  		border-radius: 1.25rem;

			padding: 2.5rem;
		}

		.skills {
			display: grid;
			grid-template-columns: repeat(3, 1fr);
			gap: 5rem;
		}

		.skills h2 {
			font-size: var(--text-2xl);
		}
	}
</style>
